# CASBOT02 训练记录

这个文档用于记录 CASBOT02 AMP 每一轮训练的背景、改动、效果和下一步判断。后续新增训练记录时，优先在最上方追加新的 `Run` 条目。

## 记录格式

每次训练按下面结构记录：

```text
## Run: 训练开始时间

状态:
命令:
基于:

上次问题:

本次修改:

预期改善:

重点观察:

阶段结果:

结论:

下一步:
```

## Run: 2026-07-13 第15次修改

状态: 代码已修改，尚未开始本轮训练和评测。

命令:

```bash
待定
```

基于:

```text
CASBOT02 当前 AMP/Leg-AMP 训练配置
```

上次问题:

- 当前训练未显式建模电机通讯延迟，实机上 action target 到电机执行存在 `0-10ms` 左右延迟风险。

本次修改:

- 第15次修改: CASBOT02 所有关节 actuator 的 position target 加入 `0-10ms` 电机通讯延迟。
- 使用 `DelayedActuatorCfg` 包裹现有 CASBOT02 position actuator，不改变 action 维度、观测维度和关节命名。
- 当前物理步长 `0.005s`，因此延迟设置为 `0-2` 个 physics step，即 `0/5/10ms`。
- 保留 `actuator_effort_limits` 随机化，并新增兼容 delayed actuator 的 effort limit 随机化函数。

预期改善:

- 提高策略对实机电机通讯延迟和 target 执行滞后的鲁棒性。
- 让动作输出不要过度依赖零延迟仿真，降低实机控制震荡风险。

重点观察:

- `action_rate` / `mean_action_acc` 是否上升。
- 低速 `vx=0.2/0.3` 是否变钝或不走。
- `yaw=0.3/0.6` 是否更难响应。
- 落地冲击和脚滑是否因延迟变大。

阶段结果:

- 未训练，未统计。

结论:

- 当前只是 sim2real 鲁棒性改动，效果需要重新训练后评测。

下一步:

- 用新配置启动训练，优先观察前 `5000/10000` 轮曲线和基础 sim2sim。

## Run: 2026-07-07 14:53:43 CST

状态: 训练进行中；当前仅生成 `model_0.pt`，暂未评测。

命令:

```bash
python scripts/train.py Casbot02-AMP-Flat --env.scene.num-envs=4096
```

基于:

```text
logs/rsl_rl/casbot02_amp_locomotion/2026-07-07_14-53-43
resume=false
actor ONNX: 320 -> 23
critic obs: 800
对比基线: logs/rsl_rl/casbot02_amp_locomotion/2026-07-03_16-33-24/model_21000.onnx
上一轮对比: logs/rsl_rl/casbot02_amp_locomotion/2026-07-06_16-54-36
```

上次问题:

- 实机走起来有脚滑。
- 有时候像是踩空，摆脚和落脚不够稳。
- 落地特别重，脚响和冲击感明显。
- `2026-07-06_16-54-36` 的 reward 虽高，但 sim2sim 姿态和小 yaw 不如 `2026-07-03_16-33-24/model_21000`。

本次修改:

- `foot_slip.weight` 从 `-0.25` 加强到 `-0.35`。
- 保留 `foot_slip.command_threshold=0.2`。
- 保留 `soft_landing.weight=-5e-4`，`command_threshold=0.2`。
- 保留 `standing_feet_slip.weight=-1.0`。
- 保留 `standing_foot_distance.weight=-5.0`。
- 启用机器人摩擦随机化: `0.4 ~ 1.2`。
- 启用 `kp/kd` 随机化: `0.85 ~ 1.15`。
- 启用电机力矩上限随机化: `0.85 ~ 1.15`。
- 启用关节摩擦随机化: `0.5 ~ 1.5`。
- 本轮仍使用 `WalkandRun_TurnBoost_v1`。

预期改善:

- 接触脚在地上蹭着走的问题应减轻。
- 对实机摩擦、电机输出、关节阻尼误差更鲁棒。
- 落地冲击可能略有改善，但本轮还没有新增专门的摆脚高度和落地竖直速度奖励。
- 如果落地仍重，下一步应新增 `swing_foot_clearance` 和 `landing_foot_vz`，不要只继续加大 `foot_slip`。

重点观察:

- 站立是否继续岔腿和滑脚。
- `vx=0.3` 是否响应，不能为了防滑变得不走。
- `vx=-0.3` 是否后退。
- `yaw=0.3/0.6` 是否转向。
- `vx=0.5,yaw=0.5` 是否能前进转弯。
- 接触力是否下降，重点看单脚 95 分位、单脚峰值、双脚总峰值，单位 `N/BW`。
- 额外观察摆脚是否太低、是否还有踩空、落地是否仍重。

阶段结果:

- 当前仅开始训练，暂无有效模型。
- 等 `model_5000.pt` 或 `model_10000.pt` 后再导出 ONNX 做 sim2sim。
- 暂不建议直接上实机测试新模型。

结论:

- 本轮改动方向是针对实机脚滑和 sim2real gap，不是针对 reward 刷高。
- 当前还不能判断是否优于 `7.3 model_21000`。
- 推荐先继续训练到 `5000/10000` 轮再评测。

下一步:

- `5000` 轮先测一次站立、`vx=0.3`、`vx=-0.3`、`yaw=0.3/0.6`、`vx=0.5,yaw=0.5`。
- 如果脚滑下降但踩空和砸地还在，下一轮加摆脚高度和触地前竖直速度惩罚。
- 如果 `vx=0.3` 或小 yaw 明显变差，说明 `foot_slip` 或随机化过强，需要回调。

## Run: 2026-07-06 16:54:36 CST

状态: 已训练到 `38000` 轮；本轮整体不如 `2026-07-03_16-33-24`，不建议继续盲训。

命令:

```bash
python scripts/train.py Casbot02-AMP-Flat --env.scene.num-envs=4096
```

基于:

```text
logs/rsl_rl/casbot02_amp_locomotion/2026-07-06_16-54-36
resume=false
actor ONNX: 320 -> 23
critic obs: 800
对比基线: logs/rsl_rl/casbot02_amp_locomotion/2026-07-03_16-33-24/model_21000.onnx
```

上次问题:

- `2026-07-03_16-33-24` 中小 yaw 仍弱，站立脚滑和脚距漂移明显。
- 但 `model_21000` 的原地转姿态、低速步态观感比本轮更自然。
- 本轮重点想解决小 yaw、站立脚滑和站立脚距问题。

本次修改:

- `ang_vel_z` 训练范围从约 `+-1.57` 缩到 `+-0.8`。
- 增加 `ang_vel_deadband=0.3`。
- `standing_feet_slip.weight` 加强到 `-1.0`。
- 保留 `standing_foot_distance.weight=-5.0`。
- 保留 `track_anchor_linear_velocity.weight=1.5, std=0.45`。
- 保留 `track_anchor_angular_velocity.weight=2.0, std=0.5`。
- 保留 `soft_landing.weight=-5e-4`。
- 本轮数据仍使用 `WalkandRun_TurnBoost_v1`。

预期改善:

- `yaw=0.3/0.6` 更容易学到。
- 站立脚滑减少，脚距漂移减少。
- `vx=0.3` 和 `vx=-0.3` 继续保持可用。
- 接触力维持较轻水平。

重点观察:

- 站立 30 秒是否岔腿、滑脚、torso 漂移。
- `vx=0.3` 是否明显前进。
- `vx=-0.3` 是否能后退。
- `yaw=0.3/0.6` 是否转向。
- `vx=0.5,yaw=0.5` 是否真的前进转弯。
- 接触力是否在合理范围，单位用 `N/BW`。

阶段结果:

- 曲线: reward `0.07 -> 52.48`，后 100 轮均值 `52.23`，没有 non-finite batch。
- 曲线: `error_vel_xy` 后 100 轮均值 `0.640`，`error_vel_yaw` 后 100 轮均值 `0.449`。
- 曲线: `landing_force_mean` 后 100 轮均值 `458N`。
- 曲线: `standing_slip_velocity_mean` 降到 `0.046`，但 `standing_foot_lateral_error` 升到约 `0.10m`。
- `model_31000` 站立 30 秒: torso 漂移约 `0.25m`，左右脚滑动约 `0.20/0.26m`，脚距横向变化约 `-0.080m`；不摔，但站立比 `7.3 model_21000` 差。
- `7.3 model_21000` 站立 30 秒: torso 漂移约 `0.03m`，左右脚滑动约 `0.03/0.03m`，明显更稳。
- `model_31000` 的 `vx=0.3`: 实际约 `0.271m/s`，达标；单脚 95 分位约 `555N / 1.12BW`，单脚峰值约 `1188N / 2.40BW`。
- `model_31000` 的 `vx=-0.3`: 实际约 `-0.245m/s`，达标；单脚 95 分位约 `639N / 1.29BW`，单脚峰值约 `1109N / 2.24BW`。
- `model_31000` 的 `yaw=0.3`: 实际约 `0.091rad/s`，不达标；脚滑约 `0.25/0.60m`，小 yaw 仍失败。
- `model_31000` 的 `yaw=0.6`: 实际约 `0.594rad/s`，达标；单脚 95 分位约 `631N / 1.27BW`，单脚峰值约 `1188N / 2.40BW`，双脚总峰值约 `1433N / 2.89BW`。
- `model_31000` 的 `vx=0.5,yaw=0.5`: 实际约 `0.476m/s, 0.466rad/s`，达标；单脚 95 分位约 `593N / 1.20BW`，单脚峰值约 `963N / 1.94BW`。
- `model_31000` 低速前进步态观感偏别扭，双脚同时接地时间偏长、摆动时间偏短；这一点不如 `7.3 model_21000`。

结论:

- 不推荐本轮作为主线继续训练。
- 本轮 reward 更高，但 sim2sim 不如 `2026-07-03_16-33-24/model_21000.onnx`。
- `vx=0.3` 和 `vx=-0.3` 基本达标。
- `yaw=0.6` 和 `vx=0.5,yaw=0.5` 可用。
- `yaw=0.3` 仍明显不行。
- 站立仍有滑脚和姿态漂移，本轮比 `7.3 model_21000` 更差。
- 接触力 95 分位大多在 `555-639N / 1.12-1.29BW`，可以接受；峰值最高到约 `1433N / 2.89BW`，偏大但不是首要问题。

下一步:

- 建议改训练框架，不建议继续这轮。
- 保留 `2026-07-03_16-33-24/model_21000.onnx` 作为当前对比基线。
- 不要只靠缩小 yaw 范围和加 deadband 解决小 yaw；本轮证明效果不好。
- 下一轮优先处理小 yaw 采样和站立脚滑/脚距约束，同时避免破坏 7.3 模型已有的自然转向姿态。
- 重点观察: `yaw=0.3` 是否能超过 `0.2rad/s`，站立脚滑是否低于 `0.1m`，低速前进双支撑时间是否变短。

## Run: 2026-07-03 16:33:24 CST

状态: 已训练到 `100000` 轮，已按每 `1000` 轮导出并评测 `100` 个 ONNX。

命令:

```bash
python scripts/train.py Casbot02-AMP-Flat --env.scene.num-envs=4096
```

基于:

```text
logs/rsl_rl/casbot02_amp_locomotion/2026-07-03_16-33-24
actor ONNX: 320 -> 23
critic obs: 800
```

上次问题:

- 上一次评测误用了世界系 `x` 位移判断前进转弯，导致误判为“倒着走”。
- 正确评测应使用机体系 `body_vx/body_vy`。
- 真实主要问题是站立脚滑、站立脚距漂移、小 yaw 命令响应偏弱。

本次修改:

- 本次没有继续改代码，只重新按机体系速度评测。
- 当前训练配置保留: `phase` 死区 `0.1`，`foot_slip.command_threshold=0.1`。
- 当前训练配置保留: `soft_landing.weight=-5e-4`。
- 当前训练配置保留: `standing_feet_slip.weight=-0.5`。
- 当前训练配置保留: `standing_foot_distance.weight=-5.0`。

预期改善:

- `vx=0.3` 应明显响应。
- `vx=-0.3` 应能后退。
- `yaw=0.6` 和 `yaw=1.0` 应能稳定转向。
- `vx=0.5,yaw=0.5` 应能边走边转。
- 站立脚滑和岔腿应比上一轮减少。

重点观察:

- 站立 30 秒: torso 漂移、脚距变化、脚底滑动、腿型偏离。
- 低速: `vx=0.2` 可不动，`vx=0.3` 必须动。
- 后退: `vx=-0.3` 是否稳定后退。
- 转向: `yaw=0.3/0.6/1.0` 的响应。
- 前进转弯: 用机体系 `body_vx` 判断，不能用世界系 `x` 位移误判。
- 接触力: 单脚 95 分位、单脚峰值、双脚总 95 分位，单位 `N/BW`。

阶段结果:

- 曲线: reward `35.1 -> 51.0`；`error_vel_xy 0.965 -> 0.649`；`error_vel_yaw 0.936 -> 0.499`；无 non-finite batch。
- 曲线: `landing_force_mean 427N -> 478N`，后期不再变轻。
- 100 个模型中: `vx=0.3` 失败 `1/100`，基本达标。
- 100 个模型中: `vx=0.5,yaw=0.5` 前进转弯失败 `0/100`，之前判断错误。
- 100 个模型中: `yaw=0.3` 响应不足 `72/100`，小 yaw 仍弱。
- 100 个模型中: 站立横向脚距变化大于 `8cm` 有 `55/100`。
- 100 个模型中: 站立脚滑大于 `0.5m` 有 `89/100`。
- 推荐模型 `model_98000`: 站立漂移 `0.056m`，脚距横向 `+0.025m`，脚滑 `0.402/0.334m`。
- 推荐模型 `model_98000`: `vx=0.3 -> 0.245m/s`，`vx=-0.3 -> -0.265m/s`。
- 推荐模型 `model_98000`: `yaw=0.3 -> 0.168rad/s`，`yaw=0.6 -> 0.515rad/s`，`yaw=1.0 -> 0.982rad/s`。
- 推荐模型 `model_98000`: `vx=0.5,yaw=0.5 -> body_vx=0.436m/s, yaw=0.449rad/s`。
- 推荐模型 `model_98000`: 运动接触力单脚 95 分位 `633N / 1.27BW`，单脚峰值 `1156N / 2.33BW`，双脚总 95 分位 `759N / 1.53BW`。

结论:

- 最推荐 `model_98000`，备选 `model_21000` 和 `model_82000`。
- 前进、后退、前进转弯整体可用。
- 小 yaw 命令偏弱，`yaw=0.3` 不够灵敏；`yaw=0.6/1.0` 明显更好。
- 运动接触力可以接受，不是当前最大问题。
- 当前最大问题是站立脚滑，其次是站立脚距漂移。
- 不建议继续按当前配置盲训更多轮，`100000` 轮已经平台。

下一步:

- 改训练框架，不建议只继续训练。
- 保留当前 4 帧历史和 320 维 actor 输入，暂时不加估计网络。
- 加强近站立脚滑约束: 优先把 `standing_feet_slip.weight` 从 `-0.5` 加到 `-1.0` 试。
- 轻微加强近站立脚距约束: `standing_foot_distance.weight` 可从 `-5.0` 试到 `-8.0`，不要过大，防止影响走路摆腿。
- 增强小 yaw 采样: 增加 `yaw=0.3~0.6` 附近命令比例，不要只靠均匀采样。
- yaw reward 暂不大幅加权，先改采样；否则可能牺牲站立和直行。
- 下一轮重点只看: 站立脚滑是否小于 `0.3m`，`yaw=0.3` 是否大于 `0.2rad/s`，`model_98000` 水平是否被超过。

## Run: 2026-07-03 16:29:07 CST

状态: 准备开始训练

命令:

```bash
python scripts/train.py Casbot02-AMP-Flat --env.scene.num-envs=4096
```

基于:

```text
logs/rsl_rl/casbot02_amp_locomotion/2026-07-03_11-15-53
```

上次问题:

- 站立时 torso 可以比较稳，但左右脚会慢慢岔开，脚底仍有滑动。
- `vx=0.3` 响应不稳定，很多模型基本不走。
- `vx=0.6` 可以前进，但实际速度偏低。
- `vx=-0.3` 后退基本没学会。
- `yaw=0.3/0.6` 原地转基本没学会。
- `vx=0.5,yaw=0.5` 多数只是往前走，几乎不转。
- 接触力比更早训练轻，`model_5000` 单脚峰值约 `1079 N`，约 `2.17 BW`，单脚 95 分位约 `565 N`，约 `1.14 BW`。

本次修改:

- `phase` 站立死区从 `0.2` 改回 `0.1`。
- `foot_slip.command_threshold` 从 `0.2` 撤回到 `0.1`。
- `track_anchor_linear_velocity.weight = 1.5`。
- `track_anchor_linear_velocity.std = 0.45`。
- `track_anchor_angular_velocity.weight = 2.0`。
- `track_anchor_angular_velocity.std = 0.5`。
- 保留 `soft_landing.weight = -5e-4`，`command_threshold = 0.2`。
- 新增 `standing_feet_slip.weight = -0.5`，只在近站立命令下生效。
- 新增 `standing_foot_distance.weight = -5.0`，只在近站立命令下生效。
- 站立脚距目标: 横向 `0.285 m`，前后 `0.0 m`。

预期改善:

- `vx=0.3` 起步响应变好。
- `yaw=0.3/0.6` 原地转响应变好。
- 站立脚底滑动减少。
- 站立左右脚岔开减少。
- 接触力保持上一轮的较轻水平。

重点观察:

- 站立 30 秒: torso 漂移、左右脚横向距离变化、左右脚前后距离变化、左右脚滑动距离。
- 前进: `vx=0.2` 可以不动，`vx=0.3` 必须明显动，`vx=0.6` 应稳定前进。
- 后退: `vx=-0.3` 是否开始后退。
- 转向: `yaw=0.3/0.6/-0.6` 是否响应。
- 前进转弯: `vx=0.5,yaw=0.5` 和 `vx=0.5,yaw=-0.5` 是否真的边走边转。
- 接触力: 单脚峰值力、单脚 95 分位力、双脚总峰值力，单位用 `N` 和 `BW`。

阶段结果:

- `3000` 轮: 待测。
- `5000` 轮: 待测。
- `8000-10000` 轮: 待测。

结论:

- 待训练后填写。

下一步:

- 如果 `10000` 轮后仍不会后退或转向，下一步改命令采样比例，不建议继续盲目大训。

## 给 Codex 的更新指令

你后续可以直接复制下面这段给我，让我更新这个文档：

```text
请更新 CASBOT02_当前问题与本轮训练方案.md。

这次训练目录是: <填 logs/rsl_rl/casbot02_amp_locomotion/xxxx>
训练开始时间是: <填时间>
训练命令是: <填命令>

请你读取训练曲线和 sim2sim 测试结果，把这次训练记录追加到文档最上方。
记录格式要保持:
状态、命令、基于、上次问题、本次修改、预期改善、重点观察、阶段结果、结论、下一步。

不要写成长篇说明书，只保留关键数字和明确判断。
重点写:
站立是否岔腿/滑脚，
vx=0.3 是否响应，
vx=-0.3 是否后退，
yaw=0.3/0.6 是否转向，
vx=0.5,yaw=0.5 是否前进转弯，
接触力是多少 N 和多少 BW，
最终推荐继续训练还是改训练框架。
```
