# CASBOT02 AMP 训练修改日志

## 基准

基于 commit `635dcae`（2026-07-07），CASBOT02 23DOF AMP 运动训练。

---

## 第一次修改（2026-07-07）

**原因**: 真机原地转弯有问题，尤其左转。

### 改动 1：去掉 `ang_vel_deadband`

- `src/tasks/amp_loco/config/casbot02/env_cfgs.py:115`: `twist_cmd.ang_vel_deadband = 0.3` 注释掉
- `src/tasks/velocity/mdp/velocity_command.py`: 整体还原为原版，删除 deadband 相关代码

deadband=0.3 截断 0~0.3 rad/s 的转弯命令（占 yaw 命令空间的 37.5%），heading 控制的小角度修正也被截断。

### 改动 2：命令范围对齐 motion 数据

| 参数 | 改前 | 改后 |
|------|:----:|:----:|
| `lin_vel_x` | `(-1.5, 2.0)` | `(-1.0, 1.0)` |
| `lin_vel_y` | `(-0.5, 0.5)` | `(-0.3, 0.3)` |
| `ang_vel_z` | `(-0.8, 0.8)` | `(-1.57, 1.57)` |

涉及文件：`amp_env_cfg.py`、`env_cfgs.py`（训练 + play 模式）

---

## 第三次修改（2026-07-08）

### 改动 1：anchor 改为 `waist_yaw_link`

- `env_cfgs.py`: `anchor_name = "waist_yaw_link"`（原 `torso`），影响速度跟踪 reward 和 AMP/critic 观测参考系

### 改动 2：AMP 风格奖励加强

- `rl_cfg.py`: `amp_task_reward_lerp = 0.25`（G1 默认 0.75 → 风格主导）、`amp_reward_coef = 0.2`

### 改动 3：转弯跟踪 reward 调低

- `env_cfgs.py`: `track_anchor_angular_velocity.weight = 1.5`（原 2.0）

### 改动 4：默认关节位置随机化

- `amp_env_cfg.py`: `joint_default_pos` DR，±0.01 rad

### 改动 5：软着陆惩罚加重

- `amp_env_cfg.py`: `soft_landing` weight -5e-4 → -1e-3，threshold 0.2 → 0.1

---

## 第四次修改（另一台电脑，2026-07-08）

基于第一次修改，增加：

| 参数 | 值 |
|------|:--:|
| `amp_reward_coef` | 0.3 |
| `anchor_name` | `waist_yaw_link` |
| `joint_default_pos` | ±0.01 rad |

与第三次对比：lerp 保持 0.75（任务主导），coef=0.3，ang_vel.weight=2.0。结果 yaw 跟踪更好但收敛更慢。

---

## 第七版修改（2026-07-09）

第五版发现风格奖励主导（lerp=0.25）导致 sim2sim 速度指令不响应。

| 相对第五版 | 改动 | 原因 |
|-----------|------|------|
| `amp_task_reward_lerp` | 0.25 → 注释（0.75） | 恢复任务主导 |
| `amp_reward_coef` | 0.2 → 0.3 | 补偿风格信号 |
| `anchor_name` | `waist_yaw_link` → `torso` | 回退 |

### 第七版新增

- `LEG_LIGHT armature`: 0.039 → 0.029（降低踝/髋 yaw 刚度）
- `soft_landing`: -1e-3 → -1e-2（着地重罚）
- `torso_mass`: `dr.body_mass`, -0.5~1.5 kg，应用在 `waist_yaw_link`

---

## 第九版修改（2026-07-10）

| 参数 | 第七版 | 第九版 |
|------|:--:|:--:|
| `track_anchor_linear_velocity.weight` | 1.5 | 2.5 |
| `track_anchor_angular_velocity.weight` | 2.0 | 2.5 |

---

## 第13版修改（2026-07-11）

### 超参统一

| 参数 | 改前 | 改后 |
|------|:--:|:--:|
| `amp_reward_coef`（全局） | 0.3 | **0.2** |
| `linear_velocity.weight` | 2.5 | **2.0** |
| `angular_velocity.weight` | 2.5 | **2.0** |

### 新增腿-only AMP 任务

新建 `Casbot02-Leg-AMP-Flat`：

- 观测去掉 10 个手臂关节 + 6 个手臂 body
- actor 200 维（原 320），AMP 105 维（原 195）
- 动作 13 维（腿+腰），网络输出 13 维
- 新建 `leg_env_cfgs.py`、`leg_rl_cfg.py`、`sim2sim_casbot02_leg_amp_onnx.py`

---

## 第15版修改（2026-07-13）

**原因**: 提高 sim2real 鲁棒性，显式建模实机电机通讯/target 执行延迟。

### 改动 1：CASBOT02 所有关节加入 0-10ms 电机通讯延迟

- `src/assets/robots/casbot02/casbot02_constants.py`: 引入 `DelayedActuatorCfg`
- 用 `DelayedActuatorCfg` 包裹原有 CASBOT02 position actuator
- 延迟对象: `position target`
- 当前物理步长 `0.005s`，因此设置为 `delay_min_lag=0`、`delay_max_lag=2`
- 对应真实时间: `0 / 5 / 10ms`
- `delay_update_period=4`，约每 `20ms` 刷新一次延迟采样
- `delay_hold_prob=0.5`，让延迟有一定时间相关性，避免每个物理步随机抖动

### 改动 2：保持 action/obs 维度不变

- 全身任务 action 仍为 `23` 维
- 下半身任务 action 仍为 `13` 维
- actor / critic / AMP 观测维度不因本次修改变化
- 本次只改变 actuator target 到实际执行之间的延迟模型

### 改动 3：兼容 actuator effort limit 随机化

- `src/tasks/amp_loco/mdp/events.py`: 新增 `effort_limits_with_delayed_actuators`
- 该函数会先 unwrap `DelayedActuator`，再对底层 actuator 做 effort limit 随机化
- `src/tasks/amp_loco/config/casbot02/env_cfgs.py`: `actuator_effort_limits` 切换到新兼容函数

### 预期影响

- 提高策略对实机电机通讯延迟的鲁棒性
- 降低零延迟仿真导致的实机控制震荡风险
- 可能让低速、小 yaw 响应变钝，需要重点观察 `vx=0.2/0.3`、`yaw=0.3/0.6`
- 可能增大 `action_rate` / `mean_action_acc`、脚滑或落地冲击，需要重新评测

---

## 第16版修改（2026-07-15）

**原因**: 下半身策略最终部署时 actor 只应依赖下肢+腰部状态，但训练时仍希望下半身适应上半身运动/惯性；对齐同事方案：AMP 和 critic 的 body 风格信息保持全身，action 仍输出 23 维。

### 改动 1：下半身 AMP 任务改为“下半身 actor obs + 23 维全身 action”

- `src/tasks/amp_loco/config/casbot02/leg_env_cfgs.py`
- `src/tasks/amp_loco/config/casbot02/leg_rl_cfg.py`
- `Casbot02-Leg-AMP-Flat` 不再把 action/actuator 限制成 13 维
- 策略 action 恢复为 `23` 维：12 个腿关节 + 1 个腰关节 + 10 个手臂关节
- actor 仍只观测下肢和腰部：单帧 `50` 维，4 帧历史后 `200` 维
- actor 的上一帧 action 只取前 `13` 维腿+腰动作，保持部署可用观测

### 改动 2：AMP 观测恢复为全身 13 个关键 body

- `src/tasks/amp_loco/config/casbot02/leg_env_cfgs.py`
- AMP body 使用 `CASBOT02_23DOF_AMP_BODY_NAMES`
- RL 配置里的 `amp_body_names` 同步改为 `CASBOT02_23DOF_AMP_BODY_NAMES`，保证 expert AMP 数据也是 `195` 维
- AMP 观测项保持：`body_pos_b`、`body_ori_b`、`body_lin_vel_b`、`body_ang_vel_b`
- AMP 维度从腿-only 的 `105` 恢复为全身 `195`
- 目的：让判别器继续约束上半身运动风格，避免下半身只学到“上半身静止”的风格

### 改动 3：critic body 信息与全身训练保持一致

- critic 的关节位置、关节速度、上一帧 action 仍只用腿+腰 `13` 维
- critic 的 `body_pos_b` 和 `body_ori_b` 使用全身 13 个 AMP key bodies
- critic 单帧维度变为 `170`，4 帧历史后 `680`
- actor / critic / AMP 的核心维度为：actor `200`，critic `680`，AMP `195`，action `23`
- `min_normalized_std` 从 13 项扩为 23 项，匹配 23 维 action

### 改动 4：新增通用上一帧 action 切片观测

- `src/tasks/amp_loco/mdp/observations.py`: 新增 `last_action_slice`
- 用于在 23 维 action 任务里，只把腿+腰前 `13` 维上一帧动作送入 actor/critic 观测
- 避免 actor 重新依赖手臂 action，同时保持网络输出可以控制全身默认/残差目标

### 改动 5：下半身 sim2sim 适配 200 输入 / 23 输出 ONNX

- `scripts/sim2sim_casbot02_leg_amp_onnx.py`
- 默认搜索 `casbot02_leg_amp_locomotion` 下的 `Casbot02-Leg-AMP-Flat_model_*.onnx`
- 观测按下半身 actor 构造：`base_ang_vel`、`projected_gravity`、`command`、`phase`、腿+腰 `joint_pos/joint_vel/last_action`
- ONNX 输出必须是 `23` 维；旧的 `13` 维 leg-only ONNX 会被显式拒绝
- sim2sim 直接用 23 维 action 生成全身 target position，上一帧 action 观测只回填前 13 维

### 预期影响

- 下半身部署观测仍然简单，只依赖腿+腰状态
- 训练时 AMP/critic 能看到上半身运动风格，有利于下半身适应上半身惯性和遥操作扰动
- 旧的第13版 leg-only `13` 输出 ONNX 不再适配新 sim2sim，需要重新训练/导出新的 `200` 输入、`23` 输出 ONNX
- 后续评测重点：上半身运动时下半身是否仍能稳定走路、站立回正是否保持默认腿型、23 维 action 是否让手臂目标产生不必要动作


2026-07-15_19-24-39  20000轮模型，转弯可以，但是会前倾














---

## 第18版修改（2026-07-16）

**原因**: 继续调整下半身部署方案，去掉策略对步态相位和腰部状态/动作的依赖；同时修正基座质量随机化作用的 link，并让 sim2sim 与当前训练观测、动作严格一致。

### 改动 1：下半身 actor/critic 改为 12 个腿关节且去掉相位

- `src/tasks/amp_loco/config/casbot02/leg_env_cfgs.py`
- actor/critic 的关节位置、关节速度和上一帧动作只保留 12 个腿关节
- actor/critic 删除 `phase` 观测，不再输入步态相位 `sin/cos`
- 单帧 actor 观测为 `45` 维：基座角速度 `3` + 重力投影 `3` + 速度指令 `3` + 腿关节位置 `12` + 腿关节速度 `12` + 上一帧腿动作 `12`
- 4 帧历史后 actor 输入为 `180` 维
- critic 保留全身 13 个关键 body 的相对位置和姿态，4 帧历史后为 `660` 维
- AMP 判别器继续使用全身 13 个关键 body，维度保持 `195`

### 改动 2：action 改为 22 维 no-waist 控制

- 新增 `CASBOT02_LEG_ONLY_JOINT_NAMES`、`CASBOT02_22DOF_NO_WAIST_JOINT_NAMES` 和对应 action scale
- 策略输出由 23 维改为 `22` 维，控制 12 个腿关节和 10 个手臂关节
- `waist_yaw_joint` 不由策略控制，保持模型默认位置
- actor/critic 的上一帧动作观测只截取前 12 维腿部动作

### 改动 3：sim2sim 严格适配 180 输入 / 22 输出

- `scripts/sim2sim_casbot02_leg_amp_onnx.py`
- 观测固定为 12 腿关节、无相位、4 帧历史
- ONNX 必须为 `180` 输入、`22` 输出，拒绝旧观测或旧 action 模型
- MuJoCo 仍保留 23 个执行器，但 waist 使用默认位置，仅把 22 维策略 action 映射到 no-waist 关节

### 改动 4：基座质量随机化 link 修正

- `src/tasks/amp_loco/config/casbot02/env_cfgs.py`
- 原 `waist_yaw_link` 质量随机化停用
- 新增 `base_mass`，作用到 MuJoCo 中对应 URDF `base_link` 的 `torso`
- 质量采用均匀加法随机化，范围由原计划的 `±1.0 kg` 收紧为 `±0.5 kg`
- `base_com` 仍作用在 `torso`，范围保持 x/y `±0.025 m`、z `±0.03 m`

### 改动 5：落地惩罚调整

- `src/tasks/amp_loco/amp_env_cfg.py`
- `soft_landing` 权重由 `-1e-2` 调整为 `-3e-3`
- 本次训练使用 `-3e-3`；后续未提交的参数试验不计入第18版

### 对应训练与初步结果

- 训练目录：`logs/rsl_rl/casbot02_leg_amp_locomotion/2026-07-16_19-52-16`
- 对应日志：`nohup_casbot02_23dof_xzzuihaobanben_0716_19_jiejuezhuanwanwenti18_leg`
- 当前模型能够前进、后退和中高速原地转向，但低速 `yaw=0.3` 响应很弱
- 站立时存在身体前倾、两脚逐渐分开和脚部累计滑动
- 后退及转向接触峰值偏高，停止后腿型恢复到默认站姿的能力不足

---

## 第19版修改（2026-07-17）

**原因**: 第18版评测发现站立时 torso 前倾、两脚逐渐分开并持续微滑，同时后退和转向的落地冲击偏高。本版加强站立腿型、脚滑和落地约束，并加入小权重的基座水平姿态惩罚。

### 改动 1：新增基座水平姿态惩罚

- `src/tasks/amp_loco/config/casbot02/env_cfgs.py`
- 新增 `flat_orientation_l2`，使用 mjlab 内置实现
- 权重设为 `-0.2`，惩罚 root projected gravity 的 x/y 分量平方和
- 目的：减小 torso 静态前倾、后仰和侧倾
- 当前对所有速度命令生效，不仅限于站立；初始权重较小，避免明显压制前进和转弯时的合理身体倾斜

### 改动 2：加强站立脚滑和脚间距约束

- `standing_feet_slip` 权重由 `-1.0` 调整为 `-2.0`
- `standing_foot_distance` 权重由 `-5.0` 调整为 `-10.0`
- `target_lateral_distance=0.285 m`、`target_fore_distance=0.0 m` 和 `command_threshold=0.2` 保持不变
- 目的：减少站立时两脚向外缓慢滑动和最终岔腿

### 改动 3：加强落地冲击惩罚

- `src/tasks/amp_loco/amp_env_cfg.py`
- `soft_landing` 权重由第18版的 `-3e-3` 调整为 `-6e-3`
- 目的：降低后退、原地转向和前进转弯时的 first-contact force 与峰值接触力

### 改动 4：扩大基座质量随机化

- `base_mass` 仍作用于 MuJoCo `torso`，即 URDF 的 `base_link`
- 均匀加法随机化范围由 `±0.5 kg` 扩大到 `±1.0 kg`
- `base_com` 随机化范围保持 x/y `±0.025 m`、z `±0.03 m`
- 注意：当前 `body_mass` 只修改质量，不同步缩放惯量；本项主要模拟基座载荷误差

### 改动 5：新增非基座刚体质量随机化

- `src/tasks/amp_loco/config/casbot02/env_cfgs.py`
- 新增 `non_base_mass` 启动事件，明确排除基座 `torso`
- 其余每个刚体的质量分别独立均匀缩放为标称值的 `0.9～1.1`
- `shared_random=False`，不同刚体不共享同一个随机系数
- 基座 `torso` 仍只由 `base_mass` 做 `±1.0 kg` 加法随机化，不会叠加本次比例随机化
- 注意：本项同样使用 `body_mass`，只改变质量，不同步缩放刚体惯量

### 改动 6：扩大默认关节位置随机化

- CASBOT02 的 `joint_default_pos` 均匀加法随机化由 `±0.01 rad` 扩大到 `±0.02 rad`
- 仍作用于 `joint_names=(".*",)` 匹配的全部关节
- 本次在 CASBOT02 专属配置中覆盖，不影响共用 `make_amp_env_cfg()` 的其他机器人任务

### 本版重点观察

- 站立 60 秒 torso pitch RMS 和最大值是否下降
- 左右脚横向距离变化是否小于 `0.02 m`
- `standing_slip_velocity_mean` 和左右脚累计滑动是否下降
- 加强水平姿态约束后，前进速度和左右转弯是否变弱或不对称
- 扩大质量随机化后，收敛速度、站立稳定性和接触冲击是否恶化
- 非基座刚体独立质量缩放后，左右步态和接触力是否出现持续不对称
- 默认关节位置扩大到 `±0.02 rad` 后，站立腿型恢复和 action 震荡是否恶化

---

## 第20版修改（2026-07-20）

**原因**: 第19版的最佳 checkpoint 仍存在站立脚滑、脚间距偏差和姿态约束不足；本版继续加强站立与水平姿态约束，并扩展动力学随机化。

### 改动 1：新增关节 armature 随机化

- `src/tasks/amp_loco/config/casbot02/env_cfgs.py`
- 新增 startup 事件 `joint_armature`，使用 mjlab `dr.joint_armature`
- 作用于全部机器人关节，按标称 armature 独立均匀缩放为 `0.95～1.05`
- `shared_random=False`，每个环境、每个关节独立采样
- 该随机化会修改 `dof_armature` 并触发 MuJoCo 动力学常量重算

### 改动 2：扩大非基座刚体质量随机化

- `non_base_mass` 继续排除 `torso`
- 其余 29 个刚体的独立质量缩放范围由第19版的 `0.9～1.1` 扩大为 `0.8～1.2`
- 基座 `torso` 仍单独使用 `base_mass=±1.0 kg`，不叠加比例质量随机化
- 注意：`body_mass` 只改变质量，不同步缩放刚体惯量

### 改动 3：扩展质心随机化 link

- `base_com` 作用对象由仅 `torso` 扩展为 `torso` 和 `waist_yaw_link`
- 两个 link 都使用局部质心加法偏移：x/y `±0.025 m`，z `±0.03 m`
- 该事件修改 `body_ipos`，不改变 link 的世界位姿

### 改动 4：继续加强站立与姿态约束

- `standing_feet_slip` 权重由 `-2.0` 调整为 `-3.0`
- `standing_foot_distance` 权重由 `-10.0` 调整为 `-15.0`
- `flat_orientation_l2` 权重由 `-0.2` 调整为 `-0.5`
- 脚距目标保持 `target_lateral_distance=0.285 m`、`target_fore_distance=0.0 m`，站立判定阈值保持 `0.2`

### 第20版保持不变的关键项

- `soft_landing=-1e-3`（2026-07-23 进一步放松着陆惩罚）
- `base_mass=±1.0 kg`
- `joint_default_pos=±0.02 rad`
- `actuator_gains` 和 `actuator_effort_limits` 仍为 `0.85～1.15`
- actor `180` 维、critic `660` 维、AMP `195` 维、action `22` 维，部署接口不变

### 本版重点观察

- armature、PD 增益、力矩上限和质量同时随机化后，收敛速度是否明显变慢
- `mean_action_acc`、`action_rate_l2` 和关节加速度是否恶化，是否出现高频震荡
- 扩大非基座质量与质心 link 后，左右步态、接触力和转向是否不对称
- 站立 60 秒的脚滑、脚间距、torso pitch 以及行走后腿型恢复是否改善
- 水平姿态权重增大后，前进加速和转弯时的合理身体倾斜是否被过度抑制

---

## 第21版修改（2026-07-24）

**原因**: 20 自由度版本（去掉两个手腕 yaw `upper_left_5_joint`、`upper_right_5_joint`）在 2026-07-20 已训练过一次（`2026-07-20_15-50-48`，action=20，actor=180，AMP=195），sim2sim 表现正常。本次正式提交代码并确认后续训练固定使用 20 维 action。

### 改动 1：action 由 22 维改为 20 维

- `src/assets/robots/casbot02/casbot02_constants.py`: 新增 `CASBOT02_20DOF_POLICY_JOINT_NAMES` 和 `CASBOT02_20DOF_POLICY_ACTION_SCALE`
- `src/assets/robots/__init__.py` / `casbot02/__init__.py`: 导出 20DOF 常量
- `src/tasks/amp_loco/config/casbot02/leg_env_cfgs.py`: action/actuator 切换为 20DOF_POLICY
- `scripts/sim2sim_casbot02_leg_amp_onnx.py`: 同步切换为 20DOF
- 22DOF 和 20DOF 常量同时保留在 `casbot02_constants.py` 中，互不影响

### 改动 2：修正 min_normalized_std

- `src/tasks/amp_loco/config/casbot02/leg_rl_cfg.py`: `min_normalized_std` 由 `[0.05]*22` 改为 `[0.05]*20`，消除截断警告

### 改动 3：进一步放松着陆惩罚

- `src/tasks/amp_loco/amp_env_cfg.py`: `soft_landing` 由 `-6e-3` 改为 `-1e-3`

### 对应训练

- 第一次 20DOF 训练（未提交代码）：`logs/rsl_rl/casbot02_leg_amp_locomotion/2026-07-20_15-50-48`（action=20, actor=180, AMP=195, critic=660, `soft_landing=-6e-3`）
- 第二次 20DOF 训练（本次提交后）：`logs/rsl_rl/casbot02_leg_amp_locomotion/2026-07-24_11-29-35`（`soft_landing=-1e-3`）

### 真机评测（2026-07-26 补充）

**0720 版本为目前最佳版本**：
- 转身完美，真机连续原地转弯无问题
- 脚步轻，动作连贯不拖沓
- **前进容易倒下**，但怀疑不是模型本身的问题，而是真机部署时给的前进指令太大，模型训练速度范围没有覆盖到
- 7月26日起后续部署速度已对齐训练范围（`lin_vel_x` 不超过 0.5 m/s）

---

nohup_casbot02_23dof_xzzuihaobanben_0724_11_jiejuezhuanwanwenti21_leg_20action   这一版本效果很差

## 第22版修改（2026-07-26）

**原因**: 真机暂时用不到后退，移除后退数据；参考第20版（0720）`soft_landing=-6e-3` 效果更好，回调着陆惩罚力度。

### 改动 1：移除后退参考动作

- 从 `WalkandRun_TurnBoost_v1/` 移除 `后退.npz`
- motion 目录剩 5 个文件：3 前进（YKS_02/04/125）+ 2 原地转

### 改动 2：速度命令去掉后退

- `src/tasks/amp_loco/amp_env_cfg.py`: `lin_vel_x` 由 `(-1.0, 1.0)` 改为 `(0, 0.5)`
- 覆盖当前三条前进动作的速度范围（0.10~0.44 m/s）
- 注意：修改的是基类 `make_amp_env_cfg()`，会影响全身训练任务

### 改动 3：回调着陆惩罚

- `src/tasks/amp_loco/amp_env_cfg.py`: `soft_landing` 由 `-1e-3` 改为 `-5e-3`
- 第20版（0720）`soft_landing=-6e-3` 效果更好，本次回调到接近该值但稍轻

---

## 第23版修改（2026-07-26）

**原因**: 加入直行1步态（从真机 .data 转换）增强前进数据，恢复后退数据；命令范围覆盖全量动作。

### 改动 1：新增直行1步态

- 从 `casbot_data/直行1步态.data` 转换为 `WalkandRun_TurnBoost_v1/直行1步态.npz`
- 速度：vx 0~0.68 m/s，中位数 0.44 m/s，1242 帧（长数据）
- 与 YKS_04 速度接近但帧数多，增强中速数据量

### 改动 2：恢复后退数据

- 恢复 `WalkandRun_TurnBoost_v1/后退.npz`（R22 移除，R23 加回）
- 速度：vx -0.79~0 m/s，中位数 -0.42 m/s

### 改动 3：命令范围覆盖全量

- `lin_vel_x` 由 `(0, 0.5)` 改为 **`(-0.8, 0.7)`**
- 前进 0.7：覆盖直行1步态（0.68）和 YKS_04（0.66）
- 后退 -0.8：覆盖后退.npz（-0.79）

### 当前 motion 数据（7 文件，含速度范围）

| 文件 | vx 范围 | vx 中位数 | 类型 |
|------|:---:|:---:|------|
| YKS_125 | 0~0.18 | 0.12 | 慢走 |
| YKS_02 | 0~0.33 | 0.18 | 中速走 |
| YKS_04 | 0~0.66 | 0.44 | 快走 |
| 直行1步态 | 0~0.68 | 0.44 | 快走（1242帧） |
| 原地右转 | ±0.31 | ~0 | 原地转 |
| 原地左转 | ±0.31 | ~0 | 原地转 |
| 后退 | -0.79~0 | -0.42 | 后退 |

---

## 第24版修改（2026-07-27）

**原因**: 真机只往前走不需要侧向和后退；回调 soft_landing 到 -6e-3（0720 最佳值）；加强速度跟踪权重。

### 改动 1：去掉后退和侧向命令

- `amp_env_cfg.py`: `lin_vel_x` 由 `(-0.8, 0.7)` 改为 `(0, 0.75)`, `lin_vel_y` 由 `(-0.3, 0.3)` 改为 `(0, 0)`
- `env_cfgs.py`: play 模式同步修改 `lin_vel_x=(0, 0.7)`, `lin_vel_y=(0, 0)`, `ang_vel_z` 统一为 `(-1.57, 1.57)`
- 再次移除 `后退.npz`（R23 恢复，R24 移除）
- 移除 `YKS_04.npz`（与直行1步态速度重叠，保留帧数更多的直行1）

### 当前 motion 数据（6 文件）

| 文件 | 帧数 | vx 中位数 | 类型 |
|------|:---:|:---:|------|
| YKS_125 | 200 | 0.12 | 慢走 |
| YKS_02 | 201 | 0.18 | 中速走 |
| 直行1步态 | 1242 | 0.44 | 快走 |
| 直行2步态 | 790 | 0.73 | 快步 |
| 原地右转 | 552 | ~0 | 原地转 |
| 原地左转 | 552 | ~0 | 原地转 |

前进总帧数从 576 → **2433**，前进:转比例为 **2.2:1**（之前 0.5:1），解决了前进数据不足导致前进效果不如转身的问题。

### 改动 2：回调 soft_landing 到 0720 基准

- `amp_env_cfg.py`: `soft_landing` 由 `-5e-3` 改为 **`-6e-3`**（与第20版 0720 完全一致）

### 改动 3：加强速度跟踪

- `env_cfgs.py`: `track_anchor_linear_velocity.weight` 由 `2.0` 提高到 **`3.0`**

---

## 第25版修改（2026-07-29）

**原因**: 加回慢速后退，扩大 armature 随机化，回调着陆惩罚和速度跟踪权重。

### 改动 1：加回慢速后退命令

- `amp_env_cfg.py`: `lin_vel_x` 由 `(0, 0.75)` 改为 **`(-0.5, 0.7)`**
- `env_cfgs.py`: play 模式 `lin_vel_x` 由 `(0, 0.7)` 改为 **`(-0.5, 0.7)`**
- 后退仅到 -0.5 m/s（R23 的 -0.8 太激进），真机安全低速后退

### 改动 2：放松着陆惩罚

- `amp_env_cfg.py`: `soft_landing` 由 `-6e-3` 改为 **`-3e-3`**
- 原因：-6e-3 可能过度压制步态自然度

### 改动 3：扩大 armature 随机化

- `env_cfgs.py`: `joint_armature` DR 范围由 `0.95～1.05` 扩大为 **`0.90～1.10`**

### 改动 4：回调速度跟踪权重

- `env_cfgs.py`: `track_anchor_linear_velocity.weight` 由 `3.0` 回调为 **`2.0`**

### 改动 5：恢复后退参考动作

- 再次加回 `后退.npz`（R24 移除，R25 恢复），vx 中位数 -0.42 m/s
- 与 `lin_vel_x` 的 -0.5 下限匹配

### 改动 6：扩大基座质量随机化

- `env_cfgs.py`: `base_mass` 范围由 `±1.0 kg` 扩大为 **`±1.5 kg`**


---

## 第26版修改（2026-07-30）

**原因**: 第25次真机效果不如第20次，落地明显更重，直行效果也差。对标0720最佳配置，收窄随机化防训练爆炸，纯前进命令匹配数据。

### 改动 1：回调着陆惩罚到0720值

- `amp_env_cfg.py`: `soft_landing` 由 `-3e-3` 改回 **`-6e-3`**
- R25 `-3e-3` 真机落地更重，恢复与0720一致

### 改动 2：收窄随机化防爆炸

- `env_cfgs.py`: `joint_armature` DR 由 `0.90~1.10` 改回 `0.95~1.05`（对齐0720）
- `env_cfgs.py`: `base_mass` 由 `±1.5` 改回 `±1.0`（对齐0720）

### 改动 3：延迟环境置零

- `env_cfgs.py`: `delay_reset_env_ratio` 由 `0.4` 改为 **`0.0`**
- 无 recovery 数据，延迟环境摔倒后填充垃圾数据

### 改动 4：纯前进命令，对齐 motion 数据

- `amp_env_cfg.py`: `lin_vel_x` 由 `(-0.5, 0.7)` 改为 **(0, 0.5)**
- 移除 `后退.npz`、`直行2步态.npz`，恢复 `YKS_04.npz`
- 最终 motion：YKS_02、YKS_125、YKS_04、直行1步态、原地右转、原地左转（6文件）

### R26 与 0720 差异

| 项目 | 0720 | R26 |
|------|:--:|:--:|
| `lin_vel_x` | (-1.0, 1.0) | (0, 0.5) |
|'数据'   删去后退，加前进1
|延迟环境配置0
| 其他所有参数 | 相同 | 相同 |

---

## 第27版修改（2026-07-31）

**原因**: 扩大前进速度范围覆盖数据，回调着陆惩罚接近0720。

### 改动 1：扩大前进命令

- `amp_env_cfg.py`: `lin_vel_x` 由 `(0, 0.5)` 改为 **(0, 1.0)**

### 改动 2：微调着陆惩罚

- `amp_env_cfg.py`: `soft_landing` 由 `-3e-3` 改为 **`-5e-3`**

### 与 0720 差异

| 项目 | 0720 | R27 |
|------|:--:|:--:|
| `lin_vel_x` | (-1.0, 1.0) | (0, 1.0) |
| `soft_landing` | -6e-3 | -5e-3 |
| `delay_reset_env_ratio` | 0.4 | 0.0 |

---

## 参数演变总表

| 参数 | 基准 | R1 | R3 | R4 | R7 | R9 | **R13** | **R15** | **R16** | **R18** | **R19** | **R20** | **R21** | **R22** | **R23** | **R24** | **R25** | **R26** |
|------|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| `ang_vel_deadband` | 0.3 | **0** | 0 | 0 | 0 | 0 | 0 | - | - | - | - | - | - | - | - | - | - | - |
| `lin_vel_x` | (-1.5,2.0) | (-1,1) | - | - | - | - | - | - | - | - | - | - | - | **(0, 0.5)** | **(-0.8, 0.7)** | **(0, 0.75)** | **(-0.5, 0.7)** | **(0, 0.5)** | **(0, 1.0)** |
| `lin_vel_y` | (-0.1,0.1) | (-0.3,0.3) | - | - | - | - | - | - | - | - | - | - | - | - | - | **(0, 0)** | - |
| `ang_vel_z` | (-0.8,0.8) | (-1.57,1.57) | - | - | - | - | - | - | - | - | - | - | - | - | - | - | - |
| `amp_task_reward_lerp` | 0.75 | - | **0.25** | 0.75 | **0.75** | - | - | - | - | - | - | - | - | - | - |
| `amp_reward_coef` | 0.1 | - | 0.2 | **0.3** | 0.3 | 0.3 | **0.2** | - | - | - | - | - | - | - | - |
| `anchor_name` | torso | - | waist | waist | **torso** | - | - | - | - | - | - | - | - | - | - |
| `ang_vel.weight` | 2.0 | - | 1.5 | 2.0 | 2.0 | 2.5 | **2.0** | - | - | - | - | - | - | - | - |
| `lin_vel.weight` | 1.0 | - | 1.5 | - | 1.5 | 2.5 | **2.0** | - | - | - | - | - | - | - | - | **3.0** | **2.0** | - |
| `delay_reset_env_ratio` | - | - | - | - | - | - | - | - | - | - | - | 0.4 | - | - | - | 0.4 | 0.4 | **0.0** |
| `LEG_LIGHT armature` | 0.039 | - | - | - | **0.029** | - | - | - | - | - | - | - | - | - | - | - |
| `joint_armature DR` | 无 | - | - | - | - | - | - | - | - | - | - | **×0.95～1.05** | - | - | - | - | **×0.90～1.10** | **×0.95～1.05** |
| `soft_landing` | -5e-4 | - | -1e-3 | - | **-1e-2** | - | - | - | - | **-3e-3** | **-6e-3** | **-1e-3** | - | **-5e-3** | - | **-6e-3** | **-3e-3** | **-6e-3** | **-5e-3** |
| `joint_default_pos` | 无 | - | ±0.01 | ±0.01 | - | - | - | - | 同事默认姿态 | - | **±0.02 rad** | - | - | - | - | - | - | - |
| `torso_mass` | 无 | - | - | - | -0.5~1.5 | - | - | - | - | 停用 waist 随机化 | - | - | - | - | - | - | - | - |
| `base_mass` | 无 | - | - | - | - | - | - | - | - | **torso ±0.5kg** | **torso ±1.0kg** | - | - | - | - | - | **torso ±1.5kg** | **torso ±1.0kg** | - |
| `non_base_mass` | 无 | - | - | - | - | - | - | - | - | - | **非 torso ×0.9～1.1** | **非 torso ×0.8～1.2** | - | - | - | - | - |
| `base_com` links | 待指定 | - | - | - | - | - | - | - | - | **torso** | - | **torso + waist_yaw_link** | - | - | - | - | - |
| `flat_orientation_l2` | 无 | - | - | - | - | - | - | - | - | - | **-0.2** | **-0.5** | - | - | - | - | - |
| `standing_feet_slip` | 无 | - | - | - | - | - | - | - | - | **-1.0** | **-2.0** | **-3.0** | - | - | - | - | - |
| `standing_foot_distance` | 无 | - | - | - | - | - | - | - | - | **-5.0** | **-10.0** | **-15.0** | - | - | - | - | - |
| 后退参考动作 | 有 | - | - | - | - | - | - | - | - | - | - | - | - | **移除** | **恢复** | **移除** | **恢复** | **移除** |
| 直行1步态 | 无 | - | - | - | - | - | - | - | - | - | - | - | - | - | **新增** | - |
| 下半身任务 | 无 | - | - | - | - | - | **13 action / AMP105** | - | **23 action / AMP195** | **22 action / actor180 / AMP195** | - | **20 action / actor180 / AMP195** (07-20首次,未提交) | **20 action / actor180 / AMP195** | - | - | - |
| 步态相位观测 | 有 | - | - | - | - | - | - | - | 有 | **移除** | - | - | - | - | - | - |
| 电机通讯延迟 | 无 | - | - | - | - | - | - | **0-10ms** | - | - | - | - | - | - | - | - |

> R=Round。`-` 表示该版本此项未变。粗体表示该版本新改动。
