2026.6.8
使用marathon机器人打通GMR框架，实现lafan和amass数据映射

2026.6.11
使用marathon机器人打通mjlab_amp框架，初步实现行走

2026.6.16
使用casbot机器人打通GMR和mjlab_amp框架，实现行走功能

2026.6.18
casbot_02机器人完成python版本sim2sim    python版本3.11，和训练环境一样
参考的蔡博的robomimic框架
2026.6.24
casbot_02机器人实现较好的前进后退，左右平移，原地转弯
前进后退和左右平移数据来自amass数据集ACCAD     原地转弯数据我自己录制的视频
开始加入摔倒爬起动作开始训练，摔倒爬起动作来自lafan数据集的fall1sub1截取406-1950

2026.6.25
完成casbot_02机器人C++版本sim2sim

2026.6.26








expert_loss = MSE(D(expert), 1)
policy_loss = MSE(D(policy), -1)
disc_loss = 0.5 * (expert_loss + policy_loss)

但 roblab 额外支持 GAN 和 WGAN：
# roblab LossType 枚举
GAN:   BCEWithLogitsLoss  # 标准GAN
LSGAN: MSE(target=±1)     # 默认
WGAN:  -mean(D_expert) + mean(D_policy)  # + output normalizer

3.4 风格奖励计算

roblab：

style_reward = dt * style_reward_scale * clamp(1 - 0.25*(d-1)^2, min=0)
total_reward = task_lerp * task_reward + (1 - task_lerp) * style_reward
# Parkour: style_reward_scale=2.0, task_lerp=0.3
# → 70% style + 30% task (乘以dt)

AMP_mjlab:

style_reward = amp_reward_coef * clamp(1 - 0.25*(d-1)^2, min=0)
total_reward = (1 - task_lerp) * disc_reward + task_lerp * task_reward
# G1 AMP: amp_reward_coef=0.1, task_lerp=0.75
# → 75% task + 25% style (无dt缩放)

关键差异：
- roblab 乘以dt（每个step_dt=0.02），AMP_mjlab 不乘dt
- roblab的 style_reward_scale=2.0 → 实际每步 style_reward = 0.02×2.0 = 0.04
- AMP_mjlab的 amp_reward_coef=0.1 → 实际每步 style_reward = 0.1
- AMP_mjlab更依赖task reward（0.75 task vs roblab 0.30 task）→ AMP_mjlab的策略更"功利"，robolab的策略更"像人"

